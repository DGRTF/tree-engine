// import elementsTreeToHashMap from "./elementsTreeToHashMap";
// import getConfig from "./commonForTests";

// test('fgdfg', () => {
//   const config = getConfig();

//   const actual = elementsTreeToHashMap(config).enumerableGroupToMap(x => x.id);

//   const actualArray = [...actual.entries()];
//   actualArray.sort((x, y) => Number(x[0] - y[0]));

//   expect(actualArray.length).toBe(6);
//   expect(actualArray.every((x, index) => x[0] === number(index + 1))).toBe(true);
// });
